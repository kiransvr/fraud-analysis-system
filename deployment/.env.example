# Copy this file to deployment/.env and set secure values.
MSSQL_SA_PASSWORD=ReplaceWithAStrongPassword123!

# Optional backend retry tuning for transient ML service failures.
ML_SERVICE_MAX_ATTEMPTS=3
ML_SERVICE_RETRY_BACKOFF_MS=50
